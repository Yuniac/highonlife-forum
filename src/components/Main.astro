---
import Card from "../components/Card.astro";
import { getCollection } from "astro:content";

const entries = await getCollection("threads");
---

<div class="w-1/2 m-auto gap-4 flex flex-col">
  {entries.map((en) => <Card data={en.data} />)}
</div>
